# Task Contract TC-20260805-13

- 標題:E4+E5 規則查詢三端接線——contracts 白名單+`POST /v1/rules/lookup`+clinician UI 規則查詢面(依 RDL-016 display-only;G4 第二單)
- 派發者(dispatcher):Claude Fable 5
- 建置者(builder):Codex GPT-5.6 Sol(provider: OpenAI;exact model id: gpt-5.6-sol;reasoning effort: xhigh)
- RA:專案負責人(RDL-009);RA 2026-08-05 明示「派 E4E5」
- 分支:claude/init-yd1kqu(基於 main = d6e1c1e)
- 派發時間:2026-08-05
- 派工單全文 SHA-256:`ccc6cf16f1f3bda455bc6a29c907a32cf07d632f04500c5cb66309106fd405eb`(v3.2 §21.5.4;原文由派發方 session 保存)

## 範圍摘要

E4:contracts 新增 `parseRuleTextLookupRequest`(白名單恰 `query`/`as_of_date`/`dataset_version`,任何未知欄位拒絕——patient-data 防火牆延伸);API 新增 `POST /v1/rules/lookup`(additive)與 meta 擴欄 `rulesDataset`,日誌零查詢內容。E5:clinician UI 雙模式分頁(示範藥品/規則逐字查詢),規則結果恆常呈現 domain 透傳之官方轉錄警語,demo 警語與聲明零變動。硬邊界:packages/domain 與生成檔、scripts/、data/governed/ 零位元組變動;既有 101 測試零修改。

## 執行結果附記(派發方記錄,2026-08-05)

- 建置 session:`019fcec0-8eab-7630-bdbd-748a36f9eeff`;tokens 132,477;一次派發完成,零退回。
- 建置者自我檢核 10/10 PASS(白名單負向、全鏈路由、日誌標記字串測試、雙模式隔離、黑名單 grep 零命中、凍結驗證、101→122 測試、雙跑一致)。
- **派發方雙重覆核**:五項檢查獨立全跑 **122/122**(10 檔;既有測試檔零修改);`git diff origin/main -- packages/domain scripts data/governed` **空**(凍結佐證);黑名單五詞獨立 grep 全域零命中;governance-scan 獨立實測 CLEAN;UI 警語呈現機制查核=**domain 原樣透傳**(App.tsx 第 180 行 `{result.warning}`,非硬編碼——單一事實來源,優於規格底線)、DEMO_WARNING 原位保留;工作樹僅允許路徑。
- **主要成果**:規則逐字查詢自此三端可用(Web/iOS/Android 經 domain 直呼;API 經 `/v1/rules/lookup`);白名單防火牆延伸至規則面;測試 101→122。UI 截圖於本環境 BLOCKED(無瀏覽器擷取),以結構描述如實在案。
- 依 read-only `.git` 慣例由派發方代 commit,authorship 保留:commit `a1e14ca`。
- 機器可讀 attestation:`.github/attestations/TC-20260805-13.json`。
