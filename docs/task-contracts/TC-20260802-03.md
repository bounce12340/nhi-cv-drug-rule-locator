# Task Contract TC-20260802-03

- 標題:governed 儲存設計與驗證模組(intake runbook Stage 2 第二單)
- 派發者(dispatcher):Claude Fable 5
- 建置者(builder):Codex GPT-5.6 Sol(provider: OpenAI;exact model id: gpt-5.6-sol;reasoning effort: xhigh)
- RA:專案負責人(RDL-009)
- 分支:claude/init-yd1kqu(基於 main = bb5d29f)
- 派發時間:2026-08-02

## 範圍摘要

為 INTAKE-APPROVE 核准後的 payload 版本化存放建立結構與驗證能力(本單不放入任何 payload):`storage-manifest/v1` schema(datasetVersion、approvalRef、sourceRegisterRefs、生效窗、files 雜湊清單、revoked);`data/governed/` 路徑約定與防穿越 helper;`computeDatasetDigest`(依 declaredName 排序之確定性資料集摘要);`approvalWording` 逐字元驗證 `INTAKE-APPROVE <datasetVersion> <datasetDigest 前 7 碼>`;`verifyStoredFileBytes`;`isEligibleForUse`(revoked 或驗證失敗一律 false)。附隔離測試(domain/contracts/api/clinician 四個 package.json 不依賴 source-intake)。允許檔案:storage.ts、storage-fixtures.ts、storage.test.ts(新增)、index.ts(僅匯出)、data/governed/README.md(新增)、docs/test-matrix.md(僅追加列)。驗收標準七條見派發原文(派發方 session 保存)。

## 執行結果附記(派發方記錄,2026-08-02)

- 建置 session:`019fc116-f812-79f0-97de-c38fcc87e91b`;tokens 112,076。
- 建置者自我檢核 7/7 PASS;派發方獨立驗證:範圍乾淨(僅六個允許檔案)、`index.test.ts` 與 `official.test.ts` diff 為空、`data/` 僅含規則 README、fixtures 紅線掃描 CLEAN、核心邏輯抽驗(語式驗證、digest 排序確定性、revoked 閘控、防穿越)無誤、81/81 測試、五項 CI 與 governance-scan 全綠。
- **零退回**,一次通過驗收。
- 依 read-only `.git` 慣例由派發方代 commit,authorship 保留:commit `f0038d7`。
- 機器可讀 attestation:`.github/attestations/TC-20260802-03.json`。
