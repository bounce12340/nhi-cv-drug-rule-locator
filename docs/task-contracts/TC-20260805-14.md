# Task Contract TC-20260805-14

- 標題:C1+C2——packages/price-comparison(DEMO_DATA_ONLY,ADR-005 語意全文)+packages/api-client(4 端點型別化薄客戶端)
- 派發者(dispatcher):Claude Fable 5
- 建置者(builder):Codex GPT-5.6 Sol(provider: OpenAI;exact model id: gpt-5.6-sol;reasoning effort: xhigh)
- RA:專案負責人(RDL-009);依 H1/H2/H6 裁示(docs/m3-price-apiclient-plan.md §5)
- 分支:claude/init-yd1kqu(基於 head c551327)
- 派發時間:2026-08-05
- 派工單全文 SHA-256:`fbc04db31f8ee1b29e35c32cd25237be661226ced56eee9a128a9d955362174f`(v3.2 §21.5.4;原文由派發方 session 保存)

## 範圍摘要

C1:全新 workspace 套件 `packages/price-comparison`(DEMO_DATA_ONLY、零 I/O 純函式)實作 ADR-005 語意全文——comparability_key 八欄位四態(DIRECTLY_COMPARABLE/CONTEXT_ONLY/NOT_COMPARABLE/INSUFFICIENT_DATA)、price_status 五態(CURRENT/FUTURE/MISSING/STALE/CONFLICT)、任一非 CURRENT 禁最低價排序、單次 ≤4 項、缺漏不補值、現行/未來分列附生效日、「健保支付價」名稱紅線、禁用語+eligibility 雙黑名單負向測試、fixtures 顯然虛構。C2:`packages/api-client` 4 端點型別化薄客戶端(錯誤契約、警語原樣透傳、零重試/快取/secrets、worker 直引整合測試)。零外部依賴;domain/apps/scripts/data 凍結。

## 執行結果附記(派發方記錄,2026-08-05)

- 建置 session:`019fcf03-3a4d-7a13-b4ad-f18dfb90cfe6`;tokens 131,195;一次派發完成,零退回。
- 建置者自我檢核 10/10 PASS(四態/排序閘門/≤4/不補值逐條測試、四類 grep 全零、透傳鎖定、122 既有零修改+33 新增=155/155、雙跑一致、lockfile 僅 workspace 連結)。
- **派發方雙重覆核**:五項檢查獨立全跑 **155/155**(13 檔);governance-scan 獨立實測 CLEAN;`git diff origin/main -- packages/domain apps scripts data` 空(凍結佐證);真實代碼形狀與雙黑名單(價格禁用語+eligibility 五詞+新/舊/核定支付價字樣)獨立 grep 全零;工作樹僅允許路徑。
- **主要成果**:比較引擎語意層完備(待價格主檔 intake 後即可接真實資料另案);api-client 公開 API=`classifyComparability`/`comparePrices`/`createApiClient{health,meta,lookupMedication,lookupRuleText}`;測試 122→155。
- 依 read-only `.git` 慣例由派發方代 commit,authorship 保留:commit `86fed47`。
- 機器可讀 attestation:`.github/attestations/TC-20260805-14.json`。
