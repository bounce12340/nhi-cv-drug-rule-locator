# Task Contract TC-20260804-11

- 標題:T3 轉錄往返保真驗證——JSONL 條文單元 vs governed 附件2 右欄逐位元往返比對+錨點分割驗證+候選交叉核對(統計式,零規則內容)
- 派發者(dispatcher):Claude Fable 5
- 建置者(builder):Codex GPT-5.6 Sol(provider: OpenAI;exact model id: gpt-5.6-sol;reasoning effort: xhigh)
- RA:專案負責人(RDL-009);依 D3「立即」慣例派發
- 分支:claude/init-yd1kqu(基於 head a4d86da)
- 派發時間:2026-08-04
- 派工單全文 SHA-256:`a570b6f887e3262f79e0004f0e560e04da8c8cf973414e84938ea6d60fa36045`(v3.2 §21.5.4;原文由派發方 session 保存)

## 範圍摘要

新增自我完備之 `scripts/rules-verify-t3.mjs`:五件輸入雜湊閘門(governed 附件2 三方全等+T2 JSONL+三候選)、以 TC-10 同法重建右欄、錨點完全分割驗證(縫隙/重疊/越界/頁欄位/序)、往返逐位元比對(串接單元 verbatim vs 右欄原文)、候選交叉核對(261/262/263 位元組等值)、schema 獨立重驗、機械導出總判定;產出 `docs/stage3/rules-transcription-t3-report.md`(僅統計,零逐字內容)。中斷韌性條款:產物先落地、自檢隨後,write-once。

## 執行過程紀錄(派發方記錄,2026-08-04;含一次退回)

- 建置 session:`019fcae9-f79d-7ec0-a886-14a75689aa08`;首輪+續跑合計 tokens 100,146+972(另有一次 CLI 旗標位置錯誤之空跑,零執行零影響)。
- **第一輪(builder 自我攔截,BLOCKED)**:腳本完成後首次報告落地,builder 隨即發現其重建器 TSV 欄側映射錯誤(修訂欄錯映至另側),往返比對基準錯為 8/16/12 行之假失敗;依派工單 §3.9 write-once 條款,builder 不覆寫已落地報告、不繼續後續驗收,誠實回報 BLOCKED 並請求授權(當時報告 SHA `b86ca2ce…d776d`;五輸入+十凍結檔複驗未動)。
- **派發方裁示**:授權一次性移除並重生該報告——未提交之建置草稿於允許路徑內迭代屬正常建置行為;§3.9 本意為中斷韌性,不凍結已知錯誤之草稿。以 `codex exec resume` 續原 session 下達修正指令。
- **第二輪(續跑,完成)**:欄側映射修正,報告重生;builder 自我檢核 9/9 PASS,總判定 **FIDELITY_VERIFIED**;探針五索引逐一實測(隔離輸出根,exit 1/報告 absent/零殘留);正常雙跑報告 SHA 一致;十凍結檔 FROZEN-OK;五項 CI+governance-scan 全綠。
- **派發方雙重覆核**:獨立重跑 ×2 報告 SHA-256 均 `92c8621a…ddde7`(與 builder 一致);探針獨立實測 exit 1、零新寫入(write-once 設計下既有已驗證報告保留於原位——與 §6.1 之調和:失敗時零產出指「零新寫入」,builder 之隔離根探針證據已滿足原字面);五輸入雜湊未動;凍結抽驗(含 262-263 script `8a9969d1…`)對版本庫 FROZEN-OK;script 82 個中文字面稽核零規則內容;紅線掃描 CLEAN;六項檢查全綠(86/86)。
- **主要結果(統計層)**:錨點分割三區段全零瑕疵;往返逐位元等值 true×3(字元數 7455/444/654,與 T2 報告完全對照);**候選交叉核對 261/262/263 位元組全等**——JSONL 可完整重組三份候選;schema 重驗 5 項 67/67。**轉錄保真已證**;JSONL 仍為草稿,待 T4 `INTAKE-APPROVE nhi-lipid-rules-structured-2026-09-01-r1 dcb6bd9`(digest `dcb6bd91…67c28`,單檔 `rules-structured.jsonl` = `9aa028b9…7117`)。
- 依 read-only `.git` 慣例由派發方代 commit,authorship 保留:commit `596517e`。
- 機器可讀 attestation:`.github/attestations/TC-20260804-11.json`。
