# TC-20260807-22:主題切換(明亮/暗黑)+介面語言切換(中/英)

- 派發方:Claude Fable 5(dispatcher/acceptor)
- 建置者:Codex GPT-5.6 Sol,reasoning effort **xhigh**(RDL-010)
- 治理依據:RA 2026-08-07 指示(逐字)「能否加入一個明亮版跟案黑版的切換鈕,另外也請幫我加入一個中文版跟英文版的切換鈕」
- 前置:**TC-21 驗收合併後**方可派出(同檔 App.tsx);基準測試數以派發時告知為準

## 1. 主題切換

- 明亮/暗黑兩主題,顯眼切換鈕;預設跟隨系統 `prefers-color-scheme`,手動選擇後以 `localStorage` 持久化(web;原生環境安全降級為 in-memory)
- 全部樣式經 theme token 導出,不得散落硬編碼色值;兩主題下警語、無病人資料聲明、頁尾義務之**對比度均達 docs/accessibility-criteria.md 目標**(報告載明抽驗色組之對比值)

## 2. 介面語言切換(中/英)

- 切換鈕 `中文 / English`;預設中文;`localStorage` 持久化
- **翻譯範圍=介面外殼**:分頁名、按鈕、輸入提示、欄位標籤、篩選鈕、狀態文字、頁尾(顯名標示與無個資聲明提供英文對照版)
- **三不翻(紅線)**:
  1. 官方逐字條文(逐字即價值,任何語言模式下原文呈現)
  2. RA 核定警語字句(治理鎖定;英文模式於警語旁加固定說明行:`Official warnings and rule text appear in their original Chinese wording.`)
  3. 資料值(中文品名、藥商名等原樣)
- **黑名單雙語適用**:英文介面文案禁止給付結論語(`eligible`、`covered`、`reimbursable`、`qualifies` 等判定詞);標籤採事實陳述(如 `Changed in this announcement` / `3-month trial list` / `Table 2 items`)
- 文案集中於單一字典結構(zh/en 鍵值對照),缺鍵 fail closed 顯示中文(不得顯示空白或鍵名)

## 3. 禁止事項(違反即 BLOCKED)

- 不得修改 `data/governed/**`、`packages/domain/src/generated/**`、`scripts/**`、`apps/api/**`、domain 之警語常數
- 既有測試零修改(如遇斷言硬編碼舊 UI 文案而衝突:**停止並回報**,不得逕改、不得以死字串騙過)
- 不得執行任何 git 指令

## 4. 驗收標準

1. 五項檢查全綠(基準+新增)
2. 主題:token 化抽查(App.tsx 無散落色值)、對比度抽驗值列表、持久化測試
3. 語言:字典完整性測試(zh/en 鍵集合相等)、三不翻之守護測試(逐字條文與警語字串不因語言模式改變)、英文黑名單掃描 0 命中、缺鍵降級測試
4. bundle 驗證:兩主題切換鈕與語言切換鈕字樣在;英文模式說明行在
5. 自我檢查報告:檔案變更、測試數增減、對比度值、bundle 驗證、五項檢查

## 5. 中斷韌性

先寫產物、後寫報告;產物 write-once。
