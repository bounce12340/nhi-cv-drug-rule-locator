# Task Contract TC-20260802-05

- 標題:governed 儲存之 CI 持續驗證測試(intake runbook Stage 5 配套)
- 派發者(dispatcher):Claude Fable 5
- 建置者(builder):Codex GPT-5.6 Sol(provider: OpenAI;exact model id: gpt-5.6-sol;reasoning effort: xhigh)
- RA:專案負責人(RDL-009)
- 分支:claude/init-yd1kqu(基於 main = 6313c85)
- 派發時間:2026-08-02
- 派工單全文 SHA-256:`見 attestation(task_contract_sha256)`

## 範圍摘要

新增 `packages/source-intake/src/governed-store.test.ts`,使 CI 每次執行都機器驗證 `data/governed/` 全部資料集:manifest 有效性(含核准語式對資料集摘要之逐字元核對)、目錄名/路徑一致性、逐檔 SHA-256+位元組長度、未宣告額外檔案之拒絕、revoked 與 eligibility 一致性;負向情境(雜湊不符/多餘檔案/revoked)以暫存目錄假資料驗證,絕不觸碰真實庫;CSV 僅讀 bytes 計算雜湊,不解析不輸出內容。`docs/test-matrix.md` 追加對應列。

## 執行結果附記(派發方記錄,2026-08-02)

- 建置 session:`019fc157-00c8-7272-bd6e-3cbded1aab0b`;tokens 55,863。
- **環境事故一次(不計退回)**:首次派發時派發方 shell 殘留 cwd 使 codex workspace root 誤設為 `data/governed/` 子目錄;建置者(session `019fc14e-8575-74f3-9482-b03bd77035ab`,tokens 69,562)正確 fail closed——拒寫範圍外、確認庫內雜湊未動、零變更回報 BLOCKED。自 repo 根重派後一次完成。
- 建置者自我檢核 11/11 PASS;派發方獨立驗證:紅線 CLEAN、governance-scan 通過、86/86 測試(6 檔)、五項 CI 全綠;範圍僅兩個允許檔案;`data/governed/` 與既有測試 diff 均為空。
- 現存資料集 `nhi-lipid-2026-09-01-r1` 經新守護測試驗證通過(4 檔+manifest)。
- 零退回;由派發方代 commit,authorship 保留:commit `a041d4b`。
- 機器可讀 attestation:`.github/attestations/TC-20260802-05.json`。
