# TC-20260809-25:補齊 v3.2 §21.2 必要檔案(CODEOWNERS + docs/runbooks/)

- 派發方:Claude Fable 5(dispatcher/acceptor)
- 建置者:Codex GPT-5.6 Sol,reasoning effort **xhigh**(RDL-010)
- 治理依據:RA 2026-08-09 指示(逐字)「除了token,其餘請繼續執行」——派發方盤點後,**唯一不需外部專業簽核、不需 RA 個別裁決、且確實可執行**之未完成項為 tracker #16 之 §21.2 檔案缺項
- 基準:`main` = `86dc9f4`;測試基準 **302 passed / 33 files**
- 範圍性質:**純治理文件**。不觸及任何程式碼、資料、查詢語意或部署設定

## 0. 背景與界線

`docs/v32-alignment-review.md` 落差 #3 記載 §21.2 三項必要檔案缺口,其中 `docs/acceptance/` **已存在**(含 phase-acceptance-report-template.md),故本單只補 **`CODEOWNERS`** 與 **`docs/runbooks/`** 兩項。

同表落差 #4(branch protection / rulesets)**不在本單範圍**:該項屬 GitHub repository 設定、會改變合併行為,須由 RA 於 GitHub 介面裁量設定,非建置者可為。

## 1. `.github/CODEOWNERS`

反映**既有**治理事實,不創設新規則:

- 預設擁有者為 repository 擁有者(RA)
- **高敏感路徑明確列出**,使其變更必然觸發擁有者審查:`data/governed/**`、`packages/domain/src/generated/**`、`docs/regulatory-decision-log.md`、`docs/spec-source-status.md`、`scripts/governance-scan.sh`、`.github/workflows/**`、`docs/task-contracts/**`
- 檔頭註解說明:CODEOWNERS **本身不強制**任何事,實際強制力來自 branch protection(尚未設定,落差 #4);本檔為其前置且獨立具備「標示誰該看」之價值
- **不得**寫入任何具體人名、email 或 GitHub 帳號以外之識別資訊;以 `@<owner>` 形式引用 repository 擁有者(實際帳號自 `git log` 之 remote 或既有文件取得,不得臆造)

## 2. `docs/runbooks/`

**不得重複造既有內容**。既有作業文件為:`docs/launch-runbook.md`(部署與網域)、`docs/phase1-intake-runbook.md`(資料入庫六階段)、`docs/backup-restore-rollback-plan.md`。

建立 `docs/runbooks/README.md` 作為**索引**,指向上述既有文件並說明各自涵蓋範圍;另新增下列**目前確實缺少**之作業手冊:

### 2.1 `credential-rotation.md`——憑證輪替

依 2026-08-07/08 之**實際事故經驗**撰寫(事實見 `docs/launch-runbook.md` §6 與 CHANGELOG):

- 憑證存放的三層對照(沿用 launch-runbook §1.1,以引用為主不重抄)
- **輪替程序**:先在新位置放入新憑證 → 驗證新憑證可用 → 才撤銷舊憑證(順序顛倒會使部署中斷)
- **撤銷驗證必須以 token ID 比對,不得以名稱比對**——本專案已發生**兩次**「宣稱已撤銷但實測仍有效」,成因為名稱認錯;取得 ID 之方法(帳戶層 token 之 verify 端點)須寫明
- **撤銷後必須驗證部署管線仍可運作**(觸發一次部署),否則下次合併才會發現憑證已死
- **紅線**:文件內不得出現任何憑證值、帳號 ID、token ID 之實際值;只寫程序與端點路徑

### 2.2 `incident-response.md`——線上異常處置

依既有實際事故撰寫(`routes` 設定導致 workers.dev 404、三層網域憑證發不出、Pages 相對路徑 ENOENT、CDN 傳播期讀到舊 bundle):

- 判定線上是否真的異常之查核順序(端點狀態 → bundle hash 四處比對 → API 煙霧)
- **CDN 傳播期之判讀**:部署後短時間內讀到舊 bundle hash **不代表部署失敗**,應等傳播後複驗再下結論,不得急於回滾
- 回滾途徑(引用 `backup-restore-rollback-plan.md`,不重抄)
- 事故登記義務:CHANGELOG 誠實登記,含成因與**自身流程瑕疵**

## 3. 禁止事項(違反即 BLOCKED)

- 不得修改任何程式碼、測試、`data/governed/**`、`packages/**`、`apps/**`、`scripts/**`、`.github/workflows/**`
- 不得變更既有文件之既有內容(僅可在 `docs/v32-alignment-review.md` 落差 #3 與 `docs/v32-planning-tracker.md` #16 之**狀態欄**如實更新,並註明 #4 仍未完成)
- 不得執行任何 git 指令
- 不得寫入任何憑證值、真實藥品代號、帳號識別資訊
- **不得宣稱 branch protection 已設定或 §21.2 已全部完成**——#4 仍開著,狀態須如實反映為部分完成

## 4. 驗收標準

1. 五項檢查全綠;**測試數維持 302**(純文件變更,不應增減)
2. `bash scripts/governance-scan.sh origin/main` 退出碼 0
3. `CODEOWNERS` 語法有效(路徑樣式可解析),涵蓋 §1 所列全部高敏感路徑
4. `docs/runbooks/` 內容**不與既有文件重複**(以引用取代重抄),且所述程序與本專案**實際發生過**的事實一致——不得虛構未發生的事故或未驗證的步驟
5. 狀態更新如實:#16 維持 PARTIAL(因 #4 未完成),不得改為完成
6. 自我檢查報告:檔案清單、與既有文件之引用關係、測試數、五項檢查

## 5. 中斷韌性

先寫產物、後寫報告;產物 write-once。

## 6. 建置環境已知限制

沙箱無 npm registry 連線。本單為純文件,不需安裝任何套件;**若判斷需要變更相依,停止並回報**。
