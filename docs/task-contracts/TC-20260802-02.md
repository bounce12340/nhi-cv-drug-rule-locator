# Task Contract TC-20260802-02

- 標題:source-intake schema v2 — 新增 `OFFICIAL_CANDIDATE` 官方類收件通道(intake runbook Stage 2 首單)
- 派發者(dispatcher):Claude Fable 5
- 建置者(builder):Codex GPT-5.6 Sol(provider: OpenAI;exact model id: gpt-5.6-sol;reasoning effort: xhigh)
- RA:專案負責人(RDL-009)
- 分支:claude/init-yd1kqu(基於 main = b634b7c)
- 派發時間:2026-08-02

## 範圍摘要

在 `packages/source-intake` 新增與 `SYNTHETIC_TEST_ONLY` 並存、互不相通的官方類候選通道:schema `source-intake/v2`、分類 `OFFICIAL_CANDIDATE`、runbook §3 對齊之八個出處欄位(sourceChannel 三型別、sourceChannelRef、announcementRef、effectiveDate、retrievedAt、retrievedBy、custodian、integrityStatement)、authority 顯式注入與交叉分類 fail-closed、RA 核准語式 `INTAKE-APPROVE <datasetVersion> <sha7>` 逐字元驗證、outcome 新欄位 `governedStorageEligible`(僅 VERIFIED+語式有效時 true);`disposition` 恆 QUARANTINED,三個釋出欄位恆 false。允許檔案:index.ts(僅新增匯出)、official.ts、official-fixtures.ts、official.test.ts(均新增)、docs/test-matrix.md(僅追加列)。完整驗收標準七條見派發原文(本檔為入 repo 之紀錄摘要,原文由派發方 session 保存)。

## 執行結果附記(派發方記錄,2026-08-02)

- 建置 session:`019fc0cb-cbd8-7471-99f7-b91cf4d23607`;tokens 109,884。建置者自我檢核 7/7 PASS;`index.test.ts` diff 為空;新增 16 項官方通道測試,全套 4 檔 64 測試通過;五項 CI exit 0。
- **驗收退回 1 次(重派第 1 次/上限 2 次)**:派發方獨立驗證發現 `official.test.ts` 一條斷言以字面常值內嵌受管制字樣——與 CI governance-scan 職責重複且會觸發掃描。退回後建置者僅移除該斷言(fix session ref `77204`;tokens 19,440),其餘斷言與檔案未動,16/16 通過。
- 本次驗收同時發現並修補派發方掃描器盲點:`scripts/governance-scan.sh` 原無法看見 untracked 檔案,本地預掃會漏報;已由派發方修補(納入 untracked 檔內容)並以本案例實證(修補後正確攔截、移除斷言後正確放行)。
- 派發方獨立重驗:範圍乾淨、`index.test.ts` 未動、64 測試、五項 CI、紅線掃描全數通過。
- 依 read-only `.git` 慣例由派發方代 commit,authorship 保留 `Codex GPT-5.6 Sol`。
- 機器可讀 attestation:`.github/attestations/TC-20260802-02.json`。
