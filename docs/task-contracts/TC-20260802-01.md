# Task Contract TC-20260802-01

- 標題:建立派工單模板 `docs/task-contract-template.md`(v3.2 §30 規劃產物 #20)
- 派發者(dispatcher):Claude Fable 5
- 建置者(builder):Codex GPT-5.6 Sol(reasoning effort: xhigh)
- RA:專案負責人(RDL-009)
- 分支:claude/init-yd1kqu(已基於最新 main;本任務為過渡分支例外,標準格式見模板第 8 節)
- 派發時間:2026-08-02

## 範圍(做什麼)

新增**一個**檔案 `docs/task-contract-template.md`:本專案的標準派工單(Task Contract)模板,供之後每一次派工複製填寫。模板必含 12 個欄位(任務識別、三方識別、範圍、非目標、允許觸碰檔案、驗收標準、檢查要求、分支、沙箱與網路、effort 等級、回報格式、attestation 欄位清單),每欄附填寫說明,文末附以本任務自身為例、明顯標示為示範的填寫範例。繁體中文為主,欄位名可附英文。

## 非目標(不做什麼)

- 不修改任何既有檔案;除 `docs/task-contract-template.md` 外不建立任何檔案。
- 不引入任何官方規則、支付價、真實藥品代碼或藥品名稱;不加入任何病人資料欄位。
- 不改動治理面檔案(.github/workflows/、CONTRIBUTING.md、docs/regulatory-decision-log.md、.claude/settings.json、.codex/config.toml)。

## 允許觸碰的檔案

- `docs/task-contract-template.md`(新增)——僅此一個。

## 驗收標準

1. 檔案存在、為有效 Markdown,包含 12 個欄位與各欄位填寫說明。
2. 文末含以 TC-20260802-01 為例的填寫範例。
3. 除該新檔案外無其他變更、無範圍外改動。
4. 內容不含任何真實健保藥品代碼樣式、價格數字表、官方給付條文文字、病人資料欄位。
5. 五項 CI 檢查在含此檔案的工作樹上全數通過。

## 執行設定

- 沙箱:`--sandbox workspace-write`;網路:關閉
- effort:xhigh(建置類,RDL-010)

---

## 執行結果附記(派發方記錄,2026-08-02)

- Codex session:`019fc09e-86de-79c2-9151-80f372868e20`;tokens used 48,557。
- 建置者自我檢核:標準 1、2、4 PASS;標準 3 因 Codex 沙箱將 `.git` 掛載唯讀而無法自行 commit(隔離機制的預期副作用),檔案以未追蹤狀態交付,工作樹除該檔外乾淨。五項 CI 建置者實跑全數 exit 0。
- 派發方處置:逐條驗收檔案內容(12 欄位、示範標示、無資料樣式)全數 PASS 後,由派發方代為 commit 並以 `--author` 保留建置者身分;內容未經任何修改。
- 建置 commit:`329bd59`(author = Codex GPT-5.6 Sol)。
- 機器可讀 attestation:`.github/attestations/TC-20260802-01.json`。
