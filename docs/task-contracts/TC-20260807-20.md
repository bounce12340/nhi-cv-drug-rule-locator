# TC-20260807-20:子集準則擴充——公告附帶清單代碼納入(F2)

- 派發方:Claude Fable 5(dispatcher/acceptor)
- 建置者:Codex GPT-5.6 Sol,reasoning effort **xhigh**(RDL-010)
- 派工日:2026-08-07
- 治理依據:RA 2026-08-07 裁示「F1 F2」之 F2
- **邊界**:本派工產出新子集候選與報告,**不入庫、不動 codegen/引擎**;入庫俟 RA `INTAKE-APPROVE`,引擎切換另俟其裁決

## 1. 問題(實測確立)

現行子集準則=「給付規定章節」含 2.6.1./2.6.2./2.6.3. token。但公告附帶清單(表二 116、ezetimibe 試用例外 14、改價明細 57;相異代碼合計 187)中有 **1 個代碼**(1 列,章節欄空白——上游標註滯後)不滿足章節準則,因此不在子集、成分查詢查不到。

## 2. 範圍

### 2.1 準則擴充 `scripts/drug-item-subset-derive.mjs`

納入條件改為:**章節 token 精確匹配 ∈ {2.6.1., 2.6.2., 2.6.3.},或 藥品代號 ∈ 公告附帶代碼集**。

- 附帶代碼集=讀取 `data/governed/nhi-lipid-2026-09-01-r1/` 四個 CSV(唯讀)之 `nhi_code` 欄聯集;**讀取前依 storage-manifest 驗證各檔 SHA-256**,不符即 fail closed
- 列序仍依來源順序;不去重、不排序、不正規化(原則不變)
- 章節 token 之精確比對邏輯**一字不動**(近似即拒不變)
- 內嵌預設期望值(子集列數/雜湊)更新為新準則之值;來源期望值(`d41cf7bf…`、224,553 列)不變

### 2.2 測試

- 既有 look-alike 排除測試(8.2.6.1. 等)**一字不改且必須續過**
- 新增:附帶代碼準則之合成測試(章節空白但代碼在附帶集 → 納入;章節空白且不在附帶集 → 排除;附帶清單檔雜湊不符 → fail closed)
- TC-18 之 CLI/refresh 測試若內嵌舊子集列數/雜湊常數,允許**僅更新該常數**,邏輯不改

### 2.3 新子集推導與 Stage 3 報告 `docs/stage3/drug-item-subset-r2-report.md`

僅計數/欄名/雜湊。**預期值(派發方已獨立量測,供交叉驗證)**:
- 新子集=**4,048 列 / 607 相異代碼**(較 r1 恰好 +1 列 +1 代碼,經由附帶準則納入)
- 187 個附帶代碼**全數**在新子集中(覆蓋驗證,反例 0)
- 章節準則所納列數與 r1 完全相同(4,047)——擴充**只增不減**
- 兩次全新推導逐位元組一致;新子集 SHA-256 與 dataset digest(沿用 `computeDatasetDigest`)載明,資料集識別 `nhi-drug-items-2026-08-07-r2`

## 3. 禁止事項(違反即 BLOCKED)

- 不得寫入 `data/governed/`;不得動 `scripts/drug-items-codegen.mjs`、生成模組、domain/api/UI
- 子集/代碼/品名/價格只存在於 gitignored `scratchpad/`;報告僅計數
- 既有 257 項測試中,除 §2.2 明示允許之常數更新外零修改
- 不得執行任何 git 指令(掃描由派發方於提交後執行)

## 4. 驗收標準

1. 五項檢查全綠(257 ± 新增測試數)
2. 新子集列數/代碼數與 §2.3 預期值完全相符;187 附帶代碼覆蓋反例 0
3. 章節準則納入集與 r1 逐列一致(只增不減之機器驗證)
4. 兩次全新推導逐位元組一致;`--check` 通過;force-hash-mismatch 與附帶清單雜湊不符兩探針皆 fail closed 零殘留
5. 自我檢查報告載明:檔案變更、測試數增減、各探針、雜湊與 digest

## 5. 中斷韌性

先寫產物、後寫報告;產物 write-once。
