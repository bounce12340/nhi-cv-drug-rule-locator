# Test matrix

| Area | Assertion | Automated evidence |
| --- | --- | --- |
| Code normalization | NFKC, trim, uppercase, spaces and hyphens normalize deterministically | `packages/domain/src/index.test.ts` |
| Near code | One changed character is never auto-corrected | `packages/domain/src/index.test.ts` |
| Ambiguity | Multiple candidates are returned without a selected default | `packages/domain/src/index.test.ts` |
| Dates/versions | Out-of-range date or unknown version fails closed | `packages/domain/src/index.test.ts` |
| Demo warning | Lookup and price status are explicitly demo-only | `packages/domain/src/index.test.ts` |
| API health | Health is structured and warns about demo data | `apps/api/src/index.test.ts` |
| API metadata | Dataset version/status can be read | `apps/api/src/index.test.ts` |
| API lookup | Exact lookup works and extra patient fields are rejected | `apps/api/src/index.test.ts` |
| Synthetic raw-byte integrity | Declared SHA-256 is calculated against original bytes and a mismatch is rejected | `packages/source-intake/src/index.test.ts` |
| Synthetic provenance | Missing or malformed synthetic source/retrieval provenance fails closed | `packages/source-intake/src/index.test.ts` |
| Synthetic authority registry | The default registry is empty; only an explicitly injected synthetic authority can proceed | `packages/source-intake/src/index.test.ts` |
| Synthetic RA state | Missing approval remains pending and an explicit rejection is rejected | `packages/source-intake/src/index.test.ts` |
| Synthetic quarantine | Every outcome is quarantined, non-publishable and contains no released payload or content | `packages/source-intake/src/index.test.ts` |
| Synthetic fixture safety | Fixtures exclude real drug-code, price, patient and official-authority identifiers | `packages/source-intake/src/index.test.ts` |
| Official schema and provenance | Schema v2 accepts only `OFFICIAL_CANDIDATE`; every Stage 1 provenance field is required and all three official source channels are covered | `packages/source-intake/src/official.test.ts` |
| Official authority isolation | The official registry defaults empty, requires explicit injection, and rejects cross-classification authorities and manifests in both directions | `packages/source-intake/src/official.test.ts` |
| Official RA approval wording | Approval is exact-match bound to dataset version and SHA-256 prefix; wrong version, digest, format, or missing wording remains pending | `packages/source-intake/src/official.test.ts` |
| Official quarantine and governed-storage eligibility | Every outcome remains quarantined and non-releasable; only verified evidence with exact approval wording is governed-storage eligible | `packages/source-intake/src/official.test.ts` |
| Official negative and fixture safety | Hash mismatch, provenance gaps, unknown authority, RA rejection, malformed inputs, and prohibited fixture patterns fail closed | `packages/source-intake/src/official.test.ts` |
| Type integrity | All packages typecheck after generated Worker types | `pnpm typecheck` |
| Web artifact | Expo static web export completes | `pnpm export:web` |
| Worker config | Generated types are current and deploy plan passes dry-run | `pnpm worker:types`, `pnpm worker:dry-run` |
| Governed storage schema | Every `storage-manifest/v1` field and nested field is required, typed, normalized to frozen null-prototype records, and malformed input fails closed | `packages/source-intake/src/storage.test.ts` |
| Governed storage approval | Approval wording exactly binds the dataset version and deterministic seven-character dataset-digest prefix; revoked or invalid manifests are ineligible | `packages/source-intake/src/storage.test.ts` |
| Governed storage integrity | Dataset digests are filename-order deterministic and each stored file must match both its SHA-256 and byte length | `packages/source-intake/src/storage.test.ts` |
| Governed storage path and isolation | Dataset directories reject traversal, and domain, contracts, API, and clinician package manifests cannot depend on `@nhi-cv/source-intake` | `packages/source-intake/src/storage.test.ts` |
| Governed storage fixture safety | Storage fixtures are visibly fictional and exclude prohibited identifier shapes | `packages/source-intake/src/storage.test.ts` |
| Governed repository storage | Every checked-in manifest, dataset directory, declared file hash and byte length, directory allowlist, and revoked eligibility state is verified; empty stores pass explicitly, while hash mismatches and extra files fail | `packages/source-intake/src/governed-store.test.ts` |
| Rule-text code generation | The authorized manifest wording, declared and actual file SHA-256, dataset digest, schema, unit count, and deterministic generated bytes are fail-closed and drift-checked | `packages/domain/src/generated-rules.test.ts`, `scripts/rules-codegen.mjs` |
| Rule-text generated integrity | All 67 units equal the governed JSONL field-for-field in source order, every verbatim digest is recomputed, the graph is deeply frozen, and header provenance is bound to the manifest | `packages/domain/src/generated-rules.test.ts` |
| Rule-text exact lookup | Sections and unit ids use deterministic NFKC/trim/case/whitespace normalization followed only by exact matching; returned units retain governed order | `packages/domain/src/rule-text-lookup.test.ts` |
| Rule-text ambiguity and near values | Table labels return every matching unit with no selected default; approximate sections, ids, and labels return no units, correction, or suggestion | `packages/domain/src/rule-text-lookup.test.ts` |
| Rule-text date, version, and review safeguards | Invalid or pre-effective dates, missing required strings, and mismatched dataset versions fail closed; every non-exact result requires manual review | `packages/domain/src/rule-text-lookup.test.ts` |
| Rule-text dual-source isolation | Demo medication output has no official text units or source tag, and official text output has no demo record, price, or source tag | `packages/domain/src/rule-text-lookup.test.ts` |
| Rule-text output vocabulary | The fixed negative vocabulary list scans engine-authored source and non-verbatim output fields while explicitly exempting official `verbatimText` and `columnLabels` | `packages/domain/src/rule-text-lookup.test.ts` |
| Rule-text transport allowlist | Exactly `query`/`as_of_date`/`dataset_version` are accepted; unknown patient-like fields and invalid types are rejected | `packages/contracts/src/rule-text-lookup.test.ts` |
| Rule-text API chain | Valid requests traverse contracts to domain and return every display-only result field | `apps/api/src/rule-text-lookup.test.ts` |
| Rule-text API rejection | Malformed JSON, unknown fields, and unsupported methods use the existing error contract | `apps/api/src/rule-text-lookup.test.ts` |
| Rule-text API fail-closed | Invalid or pre-effective dates and unknown dataset versions return no units and require manual review | `apps/api/src/rule-text-lookup.test.ts` |
| Rule-text API logging | Completion logs contain only common structured fields, status, unit count, and request ID; query text is absent | `apps/api/src/rule-text-lookup.test.ts` |
| Rule-text metadata | `/v1/meta` retains demo metadata and adds rule dataset version and effective date | `apps/api/src/rule-text-lookup.test.ts` |
| Clinician mode isolation | Demo medication and rule-text lookup render through separate mode components and only the selected result tree is mounted | `apps/clinician/App.tsx`, `pnpm export:web` |
| Clinician rule-text rendering | Every rule result places the official transcription warning first, followed by dataset metadata and per-unit `clausePath` plus `verbatimText` | `apps/clinician/App.tsx`, `pnpm typecheck`, `pnpm export:web` |
| Demo price comparability | All eight `comparability_key` attributes produce the four ADR-005 states; missing attributes fail to `INSUFFICIENT_DATA`, and only package quantity differences produce `CONTEXT_ONLY` | `packages/price-comparison/src/index.test.ts` |
| Demo price status and ordering gate | `CURRENT`, `FUTURE`, `MISSING`, `STALE`, and `CONFLICT` are derived as of a validated date; any non-current or non-direct candidate removes the price order | `packages/price-comparison/src/index.test.ts` |
| Demo price display safeguards | Current and future entries are separated with effective dates, missing or expired amounts are never filled or carried forward, and more than four candidates fails closed | `packages/price-comparison/src/index.test.ts` |
| Demo price fixture and vocabulary safety | Every fixture is visibly fictional and demo-tagged; real-code shape, price-decision copy, percentage-style copy, and the five eligibility terms are rejected while the exact demo warning is locked | `packages/price-comparison/src/index.test.ts` |
| Typed API client endpoints | Health, metadata, medication lookup, and rule-text lookup use contracts/domain-derived types and preserve warning, source tag, and manual-review fields | `packages/api-client/src/index.test.ts` |
| Typed API client failures and integration | HTTP, network, and malformed-JSON failures return typed results with no retry; direct Worker-handler integration covers GET and both lookup request chains | `packages/api-client/src/index.test.ts`, `packages/api-client/src/integration.test.ts` |
| Drug-item code generation | All four authorized CSV declarations, hashes, byte lengths, schemas, row counts, dataset digest, and deterministic generated bytes are fail-closed and drift-checked | `packages/domain/src/generated-items.test.ts`, `scripts/items-codegen.mjs` |
| Drug-item generated integrity | All 187 `nhi_code` records are merged field-for-field in source order, missing values remain `undefined`, generated provenance names RDL-019, and the record graph is deeply frozen | `packages/domain/src/generated-items.test.ts` |
| Drug-item exact code lookup | NFKC/trim/uppercase/space-and-hyphen normalization is followed only by exact code matching; a one-character difference returns no item, correction, or suggestion | `packages/domain/src/item-lookup.test.ts` |
| Drug-item names and ambiguity | Name, ingredient, and ingredient-category lookup changes only character form, case, and repeated whitespace; strength and dosage-form text are retained, and every ambiguous match is returned without selection | `packages/domain/src/item-lookup.test.ts` |
| Drug-item date, version, and review safeguards | Invalid or pre-effective dates, missing required strings, and mismatched dataset versions fail closed; every non-exact result requires manual review | `packages/domain/src/item-lookup.test.ts` |
| Drug-item output safeguards | The official source tag and exact warning are locked, absent prices remain undefined, and the eligibility-vocabulary blacklist covers engine-authored output and source | `packages/domain/src/item-lookup.test.ts` |
| Drug-item transport allowlist | Exactly `query`/`as_of_date`/`dataset_version` are accepted; unknown fields including `patient_id` and `diagnosis`, plus invalid types, are rejected | `packages/contracts/src/item-lookup.test.ts` |
| Drug-item API chain | Valid requests traverse contracts to domain; malformed JSON, unknown fields, unsupported methods, dates, and versions retain fail-closed behavior | `apps/api/src/item-lookup.test.ts` |
| Drug-item API privacy and metadata | Completion logs contain only operational status/count fields with no query content, while `/v1/meta` additively exposes item dataset version and effective date | `apps/api/src/item-lookup.test.ts` |
| Clinician drug-item rendering | The third tab renders all requested item fields, compares original and initial-review prices, labels absent prices as `本次公告未列異動`, passes through the domain warning, and links coverage-rule numbers to verbatim rule lookup | `apps/clinician/App.tsx`, `pnpm typecheck`, `pnpm export:web` |
| Drug-item master code generation | The authorized r2 manifest, source SHA-256/bytes, exact 20-column schema/order, 4,048 periods, 607 grouped codes, dataset digest, and deterministic generated bytes are fail-closed and drift-checked | `packages/domain/src/generated-drug-items.test.ts`, `scripts/drug-items-codegen.mjs` |
| Drug-item master lossless grouping | Every same-code non-period field, including both discarded URL columns, is revalidated before grouping; a synthetic mismatch fails with row locations only and produces no selected value | `packages/domain/src/generated-drug-items.test.ts` |
| Drug-item master ROC dates | Two- and three-digit ROC years use year + 1911, real calendar dates are validated, and cross-century, leap-month-end, and `9991231` sentinel cases are locked | `packages/domain/src/generated-drug-items.test.ts` |
| Drug-item master lookup invariants | Codes remain exact-only; Chinese/English names and ingredients use only approved normalization; ambiguity returns all covered candidates; versions, malformed dates, pre-history dates, finite-history tails, and interval gaps fail closed with no nearest-period fallback | `packages/domain/src/drug-item-lookup.test.ts` |
| Drug-item master transport and API | The request allowlist is exactly `query`/`as_of_date`/`dataset_version`, patient-like fields are rejected, `/v1/drug-items/lookup` preserves full history and the applicable period, logs omit query content, and `/v1/meta.drugItemsDataset` is additive | `packages/contracts/src/drug-item-master-lookup.test.ts`, `apps/api/src/drug-item-master-lookup.test.ts` |
| Drug-item announcement membership | Changed-price, three-month-trial, and table-two memberships derive from exact announcement records, remain frozen, and power factual result filters without a near-code join | `packages/domain/src/drug-item-integration.test.ts`, `apps/clinician/src/drug-item-ui-integration.test.ts` |
| Drug-item chapter enumeration | Master rows are enumerated by exact comma-delimited chapter token; multi-token values match, while `8.2.6.1.`, adjacent values, and partial values do not | `packages/domain/src/drug-item-integration.test.ts` |
| Clinician consolidated rendering | Exactly two tabs remain with drug lookup as default; master cards preserve complete price/history content and isolate the announcement values, version, warning, factual tags, and no-row state in a separate source block | `apps/clinician/src/drug-item-master-ui.test.ts`, `apps/clinician/src/consolidated-ui.test.ts`, `apps/clinician/App.tsx` |
| Clinician chapter navigation | Rule results expose chapter navigation to the drug tab, which enumerates master items through the exact-token helper and applies the factual filters | `packages/domain/src/drug-item-integration.test.ts`, `apps/clinician/src/consolidated-ui.test.ts` |
| Clinician responsive layout and footer | The pure layout decision switches at 768px; desktop uses a centered 960px two-column/table layout, mobile remains single-column, and both retain the attribution, no-personal-data statement, existing input warning, and source warnings | `apps/clinician/src/drug-item-ui-integration.test.ts`, `apps/clinician/src/consolidated-ui.test.ts`, `pnpm export:web` |

iOS and Android simulator/device tests are not in this matrix because the local prerequisites are unavailable; see the build report.
